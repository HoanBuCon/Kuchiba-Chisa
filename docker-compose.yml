# ─────────────────────────────────────────────────────────────────────────────
# Chisa AI — Docker Compose
# Runs all required infrastructure services + the application
# ─────────────────────────────────────────────────────────────────────────────
version: "3.9"

networks:
  chisa_net:
    driver: bridge

volumes:
  postgres_data:
  qdrant_data:
  redis_data:

# ─── Services ─────────────────────────────────────────────────────────────────

services:

  # ── PostgreSQL ──────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: chisa_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: chisa
      POSTGRES_PASSWORD: chisa_secret
      POSTGRES_DB: chisa_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - chisa_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chisa -d chisa_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Redis ───────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: chisa_redis
    restart: unless-stopped
    command: >
      redis-server
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru
      --save ""
      --appendonly no
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    networks:
      - chisa_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Qdrant ──────────────────────────────────────────────────────
  qdrant:
    image: qdrant/qdrant:v1.12.4
    container_name: chisa_qdrant
    restart: unless-stopped
    volumes:
      - qdrant_data:/qdrant/storage
    ports:
      - "6333:6333"   # REST API
      - "6334:6334"   # gRPC
    networks:
      - chisa_net

  # ── Chisa API ────────────────────────────────────────────────────
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: chisa_app
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://chisa:chisa_secret@postgres:5432/chisa_db
      REDIS_URL: redis://redis:6379/0

      QDRANT_URL: http://qdrant:6333
    ports:
      - "${APP_PORT:-8000}:8000"
    networks:
      - chisa_net
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      qdrant:
        condition: service_started
    command: python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1

  # ── Discord Bot ──────────────────────────────────────────────────
  discord:
    build:
      context: ./discord
      dockerfile: Dockerfile
    container_name: chisa_discord_bot
    restart: unless-stopped
    env_file: .env
    environment:
      DATABASE_URL: postgresql://chisa:chisa_secret@postgres:5432/chisa_db
      CORE_RAG_BASE_URL: http://app:8000
      CORE_RAG_CHAT_PATH: /api/v1/chat
      CORE_RAG_CLEAR_PATH_TEMPLATE: /api/v1/chat/clear/{user_id}
    networks:
      - chisa_net
    depends_on:
      postgres:
        condition: service_healthy
      app:
        condition: service_started
    command: node src/index.js


